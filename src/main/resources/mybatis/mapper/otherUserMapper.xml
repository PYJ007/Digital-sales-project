<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dajun.springbootplatform.repository.otherUserRepository">
    <insert id="insertOtherUser" parameterType="OtherUser" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into otheruser(phone, intro, address, company, pic, role, pass,password) VALUES (#{phone},#{intro},#{address},#{company},#{pic},#{role},0,#{password})
    </insert>
</mapper>
